# Value Estimates Local Context — moved

Configuration now lives in **`~/daily_reports/meta/`** and is shared with other skills (e.g. `timesheet`).

- **`~/daily_reports/meta/projects.yml`** — project map. Each entry can carry `harvest`, `gitlab`, `log_aliases`. New `gitlab` mappings are appended here when the user supplies one.
- **`~/daily_reports/meta/local-context.md`** — per-machine settings (daily reports path).
- **`~/daily_reports/meta/tickets.json`** — sidecar rollup; this skill reads from it for fast per-ticket actual-hours lookups.

See `SKILL.md` Step 0 for details. The legacy `.local-context.md` in this directory is no longer read.
