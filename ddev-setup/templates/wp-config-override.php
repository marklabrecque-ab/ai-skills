<?php
/**
 * Per-project WordPress overrides. Committed to version control.
 *
 * Included LAST by wp-config-local.php (just before wp-settings.php), so
 * anything set here wins over wp-config-ddev.php and the defaults in
 * wp-config-local.php.
 *
 * Typical use cases:
 *   - Override $table_prefix when the remote DB uses a non-standard prefix:
 *       $table_prefix = 'custom_';
 *   - Define environment-specific constants not covered by wp-config-ddev.php.
 */

// Example — uncomment and edit:
// $table_prefix = 'custom_';
